version: '3'

services:
  backend:
    build:
      context: contract
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app
    depends_on:
      - ethereum

  ethereum:
    build:
      context: contract
      dockerfile: Dockerfile
    command: 'npm run hardhat-node'
    ports:
      - "8545:8545"
